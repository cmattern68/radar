closeAirportPanel = () => {
	if($(".airport-popup").length !== 0)
		$(".airport-popup").remove();
}

closePlanePanel = () => {
	if($(".plane-popup").length !== 0)
		$(".plane-popup").remove();
}

pollEvent = () => {
}
